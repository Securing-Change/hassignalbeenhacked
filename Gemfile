source "https://rubygems.org"

# Use the GitHub Pages gem for GitHub Pages deployment
gem "github-pages", group: :jekyll_plugins

# You can lock the kramdown version if needed
gem "kramdown-parser-gfm"

# Ensure the latest version of public_suffix is used
gem 'public_suffix'

# Add "Last Updated" tag
gem 'jekyll-last-modified-at'

gem 'faraday-retry'